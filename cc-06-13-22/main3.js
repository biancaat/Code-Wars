// P: return a greeting with a name passed as an argument
// R: string
// E: turn 'Hello' to 'Hello, Mr. Spock'
// P: use template literals to return greeting with name argument

function sayHello(name) {
    return `Hello, ${name}`
  }