// P: number even or odd, can be negative or decimal
// R: true if number even, false if not. decimaals are considered uneven
// E: 2 => true, 4.6 => false, 5 => false
// P:turnary if n less than 10 return string greater than 10 return other string

function testEven(n) {
    return n % 2 == 0 ? true : false
}