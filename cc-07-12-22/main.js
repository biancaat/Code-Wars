// P: strings
// R: sum on string numbers as string
// E: ('2','3') => '5'
// P: return the string of the sum of the string numbers

function sumStr(a,b) {
    let sum = Number(a) + Number(b)
    return sum.toString()
  }