// P:
// R:Write a function which converts the input string to uppercase.
// E: ('hello','HELLO)
// P: pass arguument though function that turns a sting into all uppercase

function makeUpperCase(str) {
    return str.toUpperCase();
   }