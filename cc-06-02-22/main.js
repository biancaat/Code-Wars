// P: 2 numbers
// R: boolean
// E: checkForFactor(10,2), true
// P: turnary, if base % factor === 0 return true


function checkForFactor (base, factor) {
    return base % factor === 0 ? true : false
  }