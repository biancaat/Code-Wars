// P:
// R: We need a function that can transform a string into a number. What ways of achieving this do you know? Note: Don't worry, all inputs will be strings, and every string is a perfectly valid representation of an integral number.
// E: (stringToNumber("1234"),1234))
// P: run a method that conversta a string to a number.

function stringToNumber (str){
    return Number(str);
}

stringToNumber('37642934');