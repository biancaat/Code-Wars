// P: string
// R: return true if string ends with ending 
// E: solution('abc', 'bc') // returns true, solution('abc', 'd') // returns false
// P: use turnary and return true if str ends with ending using endsWith() method

function solution (str, ending){
    return str.endsWith(ending)
}